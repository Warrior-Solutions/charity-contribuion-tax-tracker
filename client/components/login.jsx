import React, { Component } from 'react';
import { Switch, Route } from 'react-router-dom';

const Login = (props) => {
  return (
    <div className="Login">
      <main>
        <button>Login</button>
      </main>
    </div>
  );
};


export default Login;
